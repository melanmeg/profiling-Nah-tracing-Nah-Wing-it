variables:
  k8s:
    all:
      tasks:
        - common/sysctl.yml
    lb:
      tasks:
        - lb/nginx.yml
    k8s:
      tasks:
        - common/nfs-client.yml
        - common/k8s.yml
    cp:
      tasks:
        - cp/setup.yml
    cp_join:
      tasks:
        # - cp/join.yml
    wk:
      tasks:
        - wk/join.yml
    cp_after:
      tasks:
        - cp/label-node.yml
        - cp/cilium.yml
        - cp/argocd.yml
        - cp/postgres-operator.yml
